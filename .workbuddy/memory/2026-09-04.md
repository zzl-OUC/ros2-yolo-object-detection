# 2026-09-04 工作日志

## 实验一交付整理（"一起做"：报告 + README + 视频规范 + git 提交）

- 新增 `实验报告.md`（目的/数据集/训练/部署/测试结果/结论/提交清单）。
  关键数字均先与磁盘核对：
  - 数据集 384 图 / 441 标注框（mouse 192、phone 249）；train 326 图、val 58 图。
  - 合并重训 `runs/detect/runs/train/ros2-yolo-s-3`：mAP50=0.920、mAP50-95=0.834（epoch100 行实测）。
  - PC 基线 90.0%（36/40，108ms/帧≈9.3FPS）；Jetson 实测 90.0%（36/40），稳态 29.7ms≈33.6FPS，首帧 2048ms 预热。
  - 错误案例 phone_12/15 未检出、phone_14/18 误判 mouse。
- 根 `README.md` 补充项目总览，部署细节指向 `jetson/README.md`。
- `video/` 两个 hash 名 mp4 规范重命名为 `demo_jetson_1.mp4` / `demo_jetson_2.mp4`（git 识别为 rename）。
- `.gitignore` 追加排除 `torchvision-0.16.0.tar.gz`、`cam_test_0.jpg`。
- git 本地 commit `0e1b464`（251 文件）。**冲突点**：现有 `.gitignore` 有意排除 `*.pt/*.onnx/weights/`（模型不入库），但作业要求提交模型 → 用 `git add -f weights/best.pt weights/best.onnx` 强制纳入。**未推送远程**，待用户检查确认后 push。

## 待办 / 风险
- push 需用户人工确认（纪律：未确认产物不得推远程）。
- 若用户不想把模型权重纳入 git，需 `git reset` 撤回权重文件或调整 `.gitignore`。
- `best.engine` 仍在 Jetson 板端（PC 仓库无），提交清单已注明其由板端脚本导出。

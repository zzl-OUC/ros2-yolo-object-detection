# 实验一：目标检测与识别 实验报告

## 1. 实验目的

利用 YOLO 目标检测算法，在数据集上训练一个可同时识别 **鼠标（mouse）** 与 **手机（phone）** 两类物体的检测模型，并部署到 **NVIDIA Jetson Orin NX** 嵌入式平台（ROS2 环境）实现实时检测。

验收指标：

- 能同时识别不少于 2 类物体；
- 独立测试集（≥20 个物体）正确识别率不低于 80%；
- Jetson 上实时检测速度不低于 5 FPS；
- 能保存测试结果并归档典型错误案例。

## 2. 数据集

数据集为桌面场景下的物体图像，包含两类目标：**mouse**、**phone**。标注格式为 YOLO txt，按 `images/train`、`images/val`、`labels/train`、`labels/val` 组织。

| 项目 | 数量 |
|---|---|
| 图像总数 | 384 张 |
| 标注框（实例）总数 | 441 个 |
| ├ mouse 标注框 | 192 个 |
| └ phone 标注框 | 249 个 |
| 训练集图像 | 326 张（376 框） |
| 验证集图像 | 58 张（65 框） |

## 3. 模型与训练

- **模型**：YOLO11s（Ultralytics），输入尺寸 640×640；
- **训练**：100 epoch，batch size 16，默认优化器；
- **训练结果**：
  - `mAP50` = **0.920**
  - `mAP50-95` = **0.834**
  - 原始桌面分布下 best `mAP50` = 0.961
- **模型导出链路**：
  - `weights/best.pt` —— 训练权重
  - `weights/best.onnx` —— ONNX 格式
  - `best.engine` —— TensorRT FP16 引擎


## 4. 测试结果

测试集为 40 张独立图像（20 mouse + 20 phone，与训练集不同场景的桌面照），在 Jetson 上使用 `best.engine` 实测：

| 指标 | 结果 | 要求 | 结论 |
|---|---|---|---|
| 总体识别率 | **90.0%（36/40）**  |
| mouse 类 | 20/20 | 
| phone 类 | 16/20 | |
| 稳态 FPS | **33.6 FPS** | 


**典型错误案例**（已归档至 `test_results/errors/`）：

- `phone_12.png`、`phone_15.png`：未检出（最偏分布样本，手机未置于桌面主体位置）；
- `phone_14.png`、`phone_18.png`：误判为 mouse（手机与鼠标外观/背景混淆）。

## 5. 结果视频

Jetson 实时检测录屏：

- `video/demo_jetson_1.mp4`
- `video/demo_jetson_2.mp4`


## 6. 结论

| 验收项 | 结果 |
|---|---|
| 识别类别数  | mouse + phone 两类 |
| 识别率        |  90.0%（36/40） |
| Jetson FPS  |  33.6 FPS |
| 保存结果与错误案例 |  `test_results_jetson.csv` + `test_results/errors/` |

模型可在 Jetson Orin NX + ROS2 上稳定实时运行。


## 7. 扩展实验：实例分割（YOLO-Seg）

在检测基础上，将任务从"框"升级为"像素级掩膜"，训练 YOLO11s-seg。

### 7.1 分割标注的自动生成

现有 441 个标注均为检测框（5 列 bbox），不含多边形轮廓。为避免人工重标，采用
**GrabCut 自动生成伪标注**：以现有检测框作为前景矩形提示，用 OpenCV GrabCut 抠出
目标轮廓，再经轮廓逼近得到归一化多边形（`class x1 y1 ... xn yn`）。

| 项目 | 数量 |
|---|---|
| 生成分割标注的图像 | 384 张 |
| 生成目标实例 | 441 个 |
| 退回矩形兜底 | 1 个（其余 440 个均为真实轮廓） |

生成脚本 `gen_seg_labels.py`，抽查可视化见 `seg_verify/`（mouse/phone 轮廓均贴合目标边缘）。

### 7.2 训练

- **模型**：YOLO11s-seg（114 层，1006 万参数），输入 640×640；
- **初始化**：骨干/颈部/检测头继承已训练的 `yolo11s.pt` 检测权重（499/499 层全部匹配迁移），仅 mask 原型头随机初始化；
- **训练**：100 epoch，batch 16，与检测实验同超参；
- **产物**：`weights/best_seg.pt`、`weights/best_seg.onnx`。

### 7.3 验证集结果（58 张 / 64 实例）

| 类别 | Box mAP50 | Box mAP50-95 | Mask mAP50 | Mask mAP50-95 |
|---|---|---|---|---|
| all | 0.924 | 0.892 | **0.897** | **0.857** |
| mouse | 0.967 | 0.961 | 0.967 | 0.961 |
| phone | 0.882 | 0.823 | 0.828 | 0.752 |

### 7.4 独立测试集实测（PC, RTX 4060 Laptop）

| 指标 | 结果 |
|---|---|
| 识别率 | **87.5%（35/40）**，且 35 张全部输出掩膜 |
| 稳态推理速度 | **53.9 ms ≈ 18.5 FPS** |
| 失败案例 | 未检出 mouse_04 / phone_08 / phone_12 / phone_14；误判 mouse_05→phone |

掩膜质量示例见 `test_results_seg/`（40 张测试图全量输出，含类别框、置信度与像素级掩膜）。

### 7.5 局限性说明

- 分割标注由 GrabCut **自动生成（伪标注）**，mask mAP 衡量的是与伪标注的一致性，
  不等价于人工精细标注的轮廓精度；
- 分割模型的检测精度（87.5%）略低于检测模型（90.0%），符合"多任务头 + 伪标注噪声"的预期；
- Jetson 端 TensorRT 引擎（`best_seg.engine`）尚未导出，18.5 FPS 为 PC 实测值；
  按检测模型 PC→Jetson 的加速经验（29.7ms 稳态），上板后预计仍满足 ≥5 FPS 要求。



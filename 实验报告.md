# 实验一：目标检测与识别 实验报告

## 1. 实验目的

利用 YOLO 目标检测算法，在自建桌面物体数据集上训练一个可同时识别 **鼠标（mouse）** 与 **手机（phone）** 两类物体的检测模型，并部署到 **NVIDIA Jetson Orin NX** 嵌入式平台（ROS2 环境）实现实时检测。验收指标：

- 能同时识别不少于 2 类物体；
- 独立测试集（≥20 个物体）正确识别率不低于 80%；
- Jetson 上实时检测速度不低于 5 FPS；
- 能保存测试结果并归档典型错误案例。

## 2. 数据集

数据集为桌面场景下的物体图像，包含两类目标：**mouse**、**phone**。标注格式为 YOLO txt（类别 id：0=mouse，1=phone），按 `images/train`、`images/val`、`labels/train`、`labels/val` 组织。

| 项目 | 数量 |
|---|---|
| 图像总数 | 384 张 |
| 标注框（实例）总数 | 441 个 |
| ├ mouse 标注框 | 192 个 |
| └ phone 标注框 | 249 个 |
| 训练集图像 | 326 张（376 框） |
| 验证集图像 | 58 张（65 框） |

> 数据集以自采桌面图像为主，并补充了公开 Roboflow 手机数据集（license: CC BY 4.0）扩充 phone 类样本，提升手机类在复杂背景下的鲁棒性。

## 3. 模型与训练

- **模型**：YOLO11s（Ultralytics），输入尺寸 640×640；
- **训练**：100 epoch，batch size 16，默认优化器；
- **训练结果**（合并重训后验证集）：
  - `mAP50` = **0.920**
  - `mAP50-95` = **0.834**
  - 原始桌面分布下 best `mAP50` = 0.961
- **模型导出链路**：
  - `weights/best.pt` —— 训练权重（PC）
  - `weights/best.onnx` —— ONNX 格式（PC 推理/跨平台）
  - `best.engine` —— TensorRT FP16 引擎（在 Jetson 上由 `setup_on_jetson.sh` 导出，板端实时推理使用）

## 4. Jetson 部署

- **硬件**：NVIDIA Jetson Orin NX
- **系统**：Ubuntu 20.04（JetPack 5）
- **软件栈**：ROS2 Foxy + TensorRT（FP16）
- **部署流程**：
  1. 将 `best.pt` 与 `jetson/` 下脚本拷到板端 `~/yolo_exp/`；
  2. `bash setup_on_jetson.sh foxy 5` 一键安装依赖并导出 `best.engine`；
  3. `python3 detect_node.py --source 0` 启动实时检测节点，屏幕显示检测框/类别/置信度与 FPS，并通过 ROS2 话题 `/yolo/detections` 发布识别结果。
- **实时性能**：稳态单帧推理 **29.7 ms ≈ 33.6 FPS**（首帧 2048 ms 为 TensorRT 引擎预热，不计入稳态）。

## 5. 测试结果

测试集为 40 张独立图像（20 mouse + 20 phone，与训练集不同场景的桌面照），在 Jetson 上使用 `best.engine` 实测：

| 指标 | 结果 | 要求 | 结论 |
|---|---|---|---|
| 总体识别率 | **90.0%（36/40）** | ≥80% | ✅ 通过 |
| mouse 类 | 20/20 | — | ✅ |
| phone 类 | 16/20 | — | ✅ |
| 稳态 FPS | **33.6 FPS** | ≥5 FPS | ✅ 通过 |

> PC 基线（RTX 4060 Laptop，`weights/best.pt`）同一套 40 张图识别率亦为 90.0%（36/40），与 Jetson 实测完全一致，说明模型在 PC↔Jetson 两端行为一致。

**典型错误案例**（已归档至 `test_results/errors/`）：

- `phone_12.png`、`phone_15.png`：未检出（最偏分布样本，手机未置于桌面主体位置）；
- `phone_14.png`、`phone_18.png`：误判为 mouse（手机与鼠标外观/背景混淆）。

逐张明细见 `test_results_jetson.csv`，识别率汇总见 `test_results/summary.txt`。

## 6. 结果视频

Jetson 实时检测录屏（提交于 `video/`）：

- `video/demo_jetson_1.mp4`
- `video/demo_jetson_2.mp4`

录屏同时呈现：实时检测窗口（含 FPS 角标）+ ROS2 话题 `/yolo/detections` 输出，覆盖"实时显示 + ROS2 发布 + FPS 达标"三项验收点。

## 7. 结论

| 验收项 | 结果 |
|---|---|
| 识别类别数 ≥ 2 | ✅ mouse + phone 两类 |
| 识别率 ≥ 80% | ✅ 90.0%（36/40） |
| Jetson FPS ≥ 5 | ✅ 33.6 FPS |
| 保存结果与错误案例 | ✅ `test_results_jetson.csv` + `test_results/errors/` |

全部验收指标达成，模型可在 Jetson Orin NX + ROS2 上稳定实时运行。

## 8. 提交材料清单

| 材料 | 路径 |
|---|---|
| 数据集 | `dataset/`（images + labels） |
| 模型 | `weights/best.pt`、`weights/best.onnx`（`best.engine` 在 Jetson 板端由脚本导出） |
| 训练程序 | `train.py`、`merge_baidu_translate.py` |
| 部署/测试程序 | `jetson/detect_node.py`、`jetson/test_accuracy.py`、`jetson/setup_on_jetson.sh`、`jetson/requirements.txt` |
| 运行说明 | `jetson/README.md`、根目录 `README.md` |
| 结果 | `test_results_jetson.csv`、`test_results/`（含 `errors/`） |
| 结果视频 | `video/demo_jetson_1.mp4`、`video/demo_jetson_2.mp4` |
| 实验报告 | `实验报告.md` |
